apiVersion: v1
kind: Service
metadata:
  name: agentops-api
  namespace: agentops
  labels:
    app: agentops-api
spec:
  selector:
    app: agentops-api
  ports:
    - name: http
      port: 80
      targetPort: http
  type: ClusterIP
